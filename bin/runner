#!/usr/bin/env ruby

# Remember where's the console file for code reloading
CONSOLE_FILE = __FILE__.freeze
DEFAULT_ENV = :production

require_relative '../config/app'

query = gets
clauses = []
while clause = gets do
  clauses << clause
end

puts KnowledgeBase.new(clauses).ask(query)